/**
 * Compile-time Management API types generated from the checked-in contract.
 * Runtime code must still validate security-sensitive responses.
 */
export type { components, operations, paths } from "./generated/management.js";
